import { useState } from 'react';
import { HeaderButton } from './HeaderButton';

const Header: React.FC = () => {

    return (
        <header>
            <nav>

            </nav>
        </header>
    )
}

export { Header }